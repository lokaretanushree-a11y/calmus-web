## Packages
firebase | Authentication and Firestore
recharts | Visualization for mood trends
framer-motion | Page transitions and UI animations
date-fns | Date formatting for mood entries

## Notes
Firebase config will be available in import.meta.env
Backend API at /api/analyze requires POST with { text: string }
Firestore collection 'moods' stores entries
